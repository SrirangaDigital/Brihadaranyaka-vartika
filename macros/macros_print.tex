\usepackage{array}
\usepackage{nudi}
\font\dev=dvng10 at 14pt
%\usepackage[none]{hyphenat}
\def\s{\char'263}
\def\c{\hbox{\lower8pt\hbox{\dev\char32}}}
\def\goodbreak{\par\penalty-300 }

\usepackage[papersize={140mm,215mm},textwidth=100mm,textheight=170mm,
topmargin=18mm,botmargin=11mm,leftmargin=20mm,rightmargin=20mm,
headheight=6mm,headsep=4mm,footskip=6mm]{zwpagelayout}
\usepackage{perpage}
\MakePerPage{footnote}
\usepackage{fancyhdr}
\pagestyle{fancy}
\usepackage{footmisc}
\renewcommand\contentsname{viSayAnukarxmaNikA}
\renewcommand{\headrulewidth}{0pt}
\renewcommand\chaptermark[1]{\markboth{}{#1}}

\chead[]{}
\lhead[\footnotesize\rm\thepage]{\footnotesize\bfseries } 
\rhead[\footnotesize\bfseries ]{\footnotesize\rm\thepage}
\cfoot[]{}
\rfoot[]{}

\def\mdash{ {\rm ---} }
\def\ndash{ {\rm --} }

\fancypagestyle{plain}{%
\chead[]{}
\lhead[]{}
\rhead[]{}
\cfoot[{\fontfamily{txr}\selectfont\thepage}]{{\fontfamily{txr}\selectfont\thepage}}
\renewcommand{\headrulewidth}{0pt}
}

\makeatletter
\renewcommand*\l@chapter[2]{%
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \vskip 1.0em \@plus\p@
    \setlength\@tempdima{2.2em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\hfil \nobreak\hb@xt@\@pnumwidth{\hss #2}\par
      \penalty\@highpenalty
    \endgroup
  \fi}
  
\def\@chapter[#1]#2{\ifnum \c@secnumdepth >\m@ne
                       \if@mainmatter
                         \refstepcounter{chapter}%
                         \typeout{\@chapapp\space\thechapter.}%
                         \addcontentsline{toc}{chapter}%
                                   {\protect\numberline{\bfseries\thechapter}#1}%
                       \else
                         \addcontentsline{toc}{chapter}{#1}%
                       \fi
                    \else
                      \addcontentsline{toc}{chapter}{#1}%
                    \fi
                    \chaptermark{#1}%
                    \addtocontents{lof}{\protect\addvspace{10\p@}}%
                    \addtocontents{lot}{\protect\addvspace{10\p@}}%
                    \if@twocolumn
                      \@topnewpage[\@makechapterhead{#2}]%
                    \else
                      \@makechapterhead{#2}%
                      \@afterheading
                    \fi}

\def\@makechapterhead#1{%
  \vspace*{10\p@}%
  {\parindent \z@ \centering \normalfont
    \ifnum \c@secnumdepth >\m@ne
      \if@mainmatter
       % \LARGE\bfseries \@chapapp\space \thechapter
        %\par\nobreak
        {\large\bf || shirxV dakiSxNAmUtaRyeV namaH ||}\par
      \fi
    \fi
    \interlinepenalty\@M
    \LARGE \bfseries #1\par\nobreak
\vskip 5pt
%\hrule height 2pt   
 \vskip 10\p@  
  }}
  
 \renewcommand\tableofcontents{%
    \if@twocolumn
      \@restonecoltrue\onecolumn
    \else
      \@restonecolfalse
    \fi
    \chapter*{\contentsname
        \@mkboth{%
           \contentsname}{\contentsname}}%
    \@starttoc{toc}%
    \if@restonecol\twocolumn\fi
    } 

\def\cleardoublepage{\clearpage\if@twoside \ifodd\c@page\else
\thispagestyle{empty}\newpage\if@twocolumn\hbox{}\newpage\fi\fi\fi}
    
\makeatother

\newcounter{shloka}[chapter]

\def\shloka{\bgroup\refstepcounter{shloka}\label{\thechapter shlokano\theshloka}\noindent\raggedright\leftskip=17pt\bgroup\bfseries\ignorespaces}
\def\endshloka{\hfill||$\thinspace${\fontfamily{txr}\fontsize{10}{12}\selectfont\theshloka}$\thinspace$||\egroup\egroup\par}

\def\shl{\bgroup\noindent\raggedright\leftskip=17pt\bgroup\bfseries\ignorespaces\obeylines}
\def\endshl{\par\egroup\egroup\medbreak}

\def\kandikeshl{\bgroup\noindent\sl\bfseries\ignorespaces\obeylines\fontsize{12}{14}\selectfont}
\def\endkandikeshl{\par\egroup\medbreak}

\def\artha{\bgroup\ignorespaces}%\raggedright}
\def\endartha{\par\egroup\medbreak}

\long\def\kandike#1{\goodbreak{\noindent\normalfont\large\bfseries #1\addcontentsline{toc}{subsection}{{$\bullet$ #1}}}\par\medbreak}

\def\vishaya{\futurelet\new\macA}

\def\macA{\ifx\new[\let\next=\vishayafirst 
\else
  \let\next=\vishayasecond
\fi 
\next}

\long\def\brahmana#1{\goodbreak{\bf\noindent #1}\addcontentsline{toc}{subsection}{$\bullet$ #1}\par\medbreak}
\long\def\vishayafirst[#1]#2{\goodbreak{\bf\noindent #2}\par\medbreak}
\long\def\vishayasecond#1{\goodbreak{\bf\noindent #1}\par\medbreak}
\long\def\stext{\textbf{(saMsakxqqta sholxVka/maMtarx) }}

\long\def\chapterartha#1{\begin{center}#1\end{center}}
\long\def\endchapter#1{\begin{center}\bf #1\end{center}}
\long\def\endchapterartha#1{\begin{center}#1\end{center}}
\def\uvaca#1{\goodbreak\centerline{{\large\textbf{#1}}}\medskip\par}
\input dnmacs
\tolerance=4000
\defaulthyphenchar=32


